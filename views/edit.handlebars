<section class="hero has-background-grey-lighter">
    <div class="hero-body">
        <div class="container">
            <div class="columns is-centered">
                <div class="column">
                    <form action="" class="box">
                        <label style="text-align: center; font-size: 200%; border-bottom: 1px solid black" class="label is-large" for="" class="label">Edit Post 📝</label>
                        <div class="field">
                            <label style="font-size: 125%" for="" class="label">Subject / Title 🗃️</label>
                            <div class="control">
                                <input type="text" placeholder="www.ssh.com...Blog Git before you even start...🎤" class="input subject-input"
                                    value="{{postData.title}}" aria-required="true"/>
                            </div>
                        </div>
                        <div class="field-body">
                            <div class="field">
                                <label style="font-size: 150%; text-align: center; padding-top: 2.5%; border-bottom: dashed 1px black" for="" class="label">Content 🗄️</label>
                                <div class="control" style="padding-top: 2.5%">
                                    <div class="card-img-top">
                                        <label for="" class="label">Image 🎞️</label>
                                            <img src="{{image}}" 
                                            placeholder="Upload Image...📸" alt="...">
                                    </div>
                                    <textarea
                                        class="textarea content-input"
                                        placeholder="Blog Git before you even start...🖥️" aria-required="true">
                                        {{postData.content}}
                                    </textarea>
                                </div>
                            </div>
                        </div>
                        <div class="field is-grouped is-grouped-right mt-3">
                            <button class="button is-success edit-submit">
                                Submit 📬
                            </button>
                        </div>
                        <div class="control">
                            <button id="upload_widget" class="cloudinary-button">
                                Upload Image 📥
                            </button>
                            <script src="https://upload-widget.cloudinary.com/global/all.js" type="text/javascript"></script> 
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
    <div class="current-post-id is-invisible">{{postData.id}}</div>
</section>
<script src="/js/edit.js"></script>